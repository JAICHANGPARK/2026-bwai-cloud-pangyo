---
description: Generate Flutter Dashboard Code
---

1. Read the rules in [gemma4_flutter.md](file:///Users/jaichang/Documents/GitHub/2026-bwai-cloud-pangyo/.agents/rules/gemma4_flutter.md).
2. Write or update the Flutter single-file Dart script at `hands-on/flutter_app.dart`.
3. Use only allowed packages (like `flutter_riverpod`, `fl_chart`, `google_fonts`, etc.) and standard widgets.
4. Avoid local asset files; load images via network and fonts via google_fonts.
5. Correctly clean up heavy resources in `dispose()`.
